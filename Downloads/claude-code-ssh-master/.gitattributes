# Force LF line endings for all files that run inside the Linux container
* text=auto eol=lf

# Ensure shell scripts are always LF
*.sh text eol=lf

# s6-overlay service files must be LF
s6-overlay/** text eol=lf

# Config files must be LF
sshd_config text eol=lf
fail2ban/** text eol=lf
banner.txt text eol=lf
supervisord.conf text eol=lf

# Dockerfile
Dockerfile text eol=lf
.dockerignore text eol=lf

# Keep Windows line endings for Windows-only files
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
